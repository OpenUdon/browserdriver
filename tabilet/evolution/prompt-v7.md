# Direction v7

Implement the existing UWS registration 1.1 contract generically. Keep ordinary
inputs and credentials in one accepted private runtime snapshot. W8M is a
consumer fixture, with no target behavior in the driver. Preserve older
protocols, one submit, exact approval identity and joined teardown.
