# Verification initial-response and diagnostic candidate v10

M13.7 preserves driver v6/UWS authority and adds the documented Turnstile initial
undefined response as pending only for a bound, empty, never-ready widget.
Response loss/replacement after readiness invalidates approval.

Internal diagnostics v3 expose only a closed return-kind label. Fixture report
v3 adds a bounded lifecycle trace, omitted count and separately retained first
error. It distinguishes render/execute exceptions, execute rejection, provider
callbacks and local trigger/completion errors. Return values, widget handles,
callback arguments and exception prose remain in the page. Closed reduction
reads properties once before validating; settlement never grants readiness.

Historical reports/readers, adopted bytes and consumed attempts are immutable.
A successor validator/supervisor binds v3 explicitly. The immediate deliverable
is one separately authorized reCAPTCHA approved_submit diagnostic proposal;
provider acceptance, publication and renewed qualification/adoption remain gates.
