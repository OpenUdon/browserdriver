# Result v7

Protocol v5 independently validates typed and conditional input definitions,
accepted snapshots and named checkpoint updates. It applies native controls,
clears inactive values using the original control kind, and binds submit
approval to the current private revision and digest. The inherited UWS 1.1
contract stays unchanged. Offline and Chromium qualification cover both v4
and v5, mutation denial, own-property field names, uncertainty and privacy.
